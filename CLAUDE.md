# CLAUDE.md

Read [`AGENTS.md`](AGENTS.md) and follow it — it is the setup runbook for this
repo (agent entry point, rules, order of work, and the acceptance criteria).

Quick orientation:

- `README.md` — the full blueprint (four layers, failure modes, security, ops)
- `QUICKREF.md` — one-page commands, endpoints, and env knobs
- `SETUP-PROMPT.md` — the same install instructions as a copy-paste prompt
- `scripts/preflight.sh` — check the machine before you start
- `scripts/verify-memory.sh` — prove the memory layer works end to end
- `configs/` — copy-ready env templates, engine skeletons, MCP and sync units
